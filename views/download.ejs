<!DOCTYPE html>
<html>
<head>
    <title>File Upload and Download</title>
    
</head>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<style>

    .links li strong {
        margin: 10px 0;
        
    }

    .links li a {
        margin-left: 15px;
    }
</style>
<body>

    
<h2 class="mt-5" style="margin-left: 35px; margin-bottom: 10px;">Available Files for Download</h2>
    <ul>
        <% if (files.length > 0) { %>
            <% files.forEach(function(file) { %>
                <div class="links">
                <i class="fa fa-file"></i>
                <li> <strong>Title:</strong> <%= file.title %><br>
                    <strong>Description:</strong> <%= file.description %><br>
                    <strong>Category:</strong> <%= file.category %><br>
                    <br/>
                    <a href="/download/<%= user.username %>/<%= encodeURIComponent(file.filename) %>" class="btn btn-outline-primary">Download</a> <%= file %> </a>
                </li>
               
                </div>
            <% }); %>
        <% } else { %>
            <li>No files available for download</li>
        <% } %>
    </ul>
</body>
</html>